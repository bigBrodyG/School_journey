#include <stdio.h>
#include <stdlib.h>
float somma(float a, float b){
    float res=a+b;
    printf("La somma di %f e %f è = %f", a, b, res);
    return 0;
}
float diff(float a, float b){
    float res=a-b;
    printf("La somma di %f e %f è = %f", a, b, res);
    return 0;
}
float molt(float a, float b){
    float res=a*b;
    printf("La somma di %f e %f è = %f", a, b, res);
    return 0;
}
float divis(float a, float b){
    float res=a/b;
    printf("La somma di %f e %f è = %f", a, b, res);
    return 0;
}